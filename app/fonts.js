import { Open_Sans } from 'next/font/google'
import { Phudu } from 'next/font/google'

export const text_font = Open_Sans({ subsets: ['latin'] })
export const header_font = Phudu({ subsets: ['latin'] })