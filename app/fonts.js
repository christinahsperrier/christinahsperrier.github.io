import { Poppins } from 'next/font/google'
import { Phudu } from 'next/font/google'

export const text_font = Poppins({ subsets: ['latin'], weight: ['400'] })
export const header_font = Phudu({ subsets: ['latin'] })